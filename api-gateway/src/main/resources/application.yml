server:
  port: 8080

spring:
  application:
    name: api-gateway

  cloud:
    gateway:
      routes:
        - id: orchestrator
          uri: http://mcp-orchestrator:8085
          predicates:
            - Path=/chat